import {StyleSheet} from 'react-native';

export const errorStyling = StyleSheet.create({
    errorBanner: { backgroundColor: '#ffebee', padding: 10, borderRadius: 5, marginBottom: 15 },
    errorText: { color: '#c62828', fontSize: 14 },
});